# IESKF prior-reference illustration v2

- Mode: built-in image generation, edit of the existing local PNG.
- Input/edit target: `fig/ieskf_nominal_iteration_ipad.png`
- Intended output: `fig/ieskf_nominal_iteration_ipad_v2.png`
- Purpose: distinguish the fixed prior expressed around the current nominal from the estimated correction applied to that nominal, using the deck's additive coordinates and notation.

## Initial prompt

Use case: scientific-educational
Asset type: wide 2.35:1 raster illustration for a Korean xaringan IESKF lecture slide.
Input image 1: EDIT TARGET and STYLE REFERENCE. Redraw this diagram, preserving its clean iPad handwritten style, white background, smooth black convex observation curve, colored contact points and tangents, and red → blue → green sequence of nominal states moving leftwards. Replace its annotations/equations to explain the previously missing prior-offset term. This is not a photo.
Primary request: show exactly how the ORIGINAL predicted nominal minus the CURRENT nominal differs from the estimated error correction. Include the full j=1 IESKF update equations. No new shorthand residual r or S or z-hat.

Layout: generous clean white margins, large legible handwriting/math, entire drawing in frame. Wide landscape. Upper 56%: one observation plot. Middle 16%: TWO DISTINCT horizontal arrow rows. Lower 28%: two full equations and a short flow caption. Keep labels from colliding with lines. Do not use an inset magnifier; clarity of the main arrows is the focus.

Plot geometry: horizontal x axis, vertical h(x) axis. A smooth increasing convex black curve. On this EXACT SAME curve place rightmost red xhat_{t,0}=xhat_t, middle blue xhat_{t,1}, and left green xhat_{t,2}. Dotted vertical projections from each colored curve point to the x-axis. Position green at roughly 35% of plot width, blue at 57%, red at 85%. Draw three straight true tangent segments H_{t,0}, H_{t,1}, H_{t,2}, respectively red, blue, green. Each must pass exactly through its corresponding curve point and have the local slope, with no floating dots or gaps. These dots on the curve represent h(xhat,0), NOT the unknown true state. Label the curve "h(x,0)" and horizontal coordinate axis "x". Label the three nominal x positions UNDER the axis, with visible hats on ALL three x labels.
Header, exactly: "같은 t, 같은 측정과 처음 prior"
Near red point, exactly: "처음 prior 고정"
Near blue point, exactly: "새 nominal에서 H, K 재계산"
Near green point, exactly: "보정 후 다시 선형화"

CRITICAL TWO ARROWS BELOW AXIS, same horizontal scale and both starting at the BLUE current nominal projection:
1) Upper arrow row: orange arrow points RIGHT from blue position to red position; endpoint exactly under original red prior. Label above or within row in large orange math: "\hat{x}_t-\hat{x}_{t,1}". Short Korean explanation to the left or below: "현재 nominal 기준의 prior mean". This is a reference difference, NOT a state update moving back to the prior. Use "기준 차이" as a small label if useful.
2) Lower arrow row: blue arrow points LEFT from blue position to green position; endpoint exactly under next green nominal. Label in blue math: "\widehat{\delta x}_{t,1}". Short Korean explanation: "이번에 추정한 보정량". This is the actual update applied to the nominal.
Do not draw any arrow from red to green here. Distinguish direction and meaning unmistakably. Dotted guide lines may extend down faintly to align endpoints.

Bottom equations, absolutely faithful, elegant mathematical handwritten typesetting, each equation on ONE horizontal line. Use clear hats and correct t,1 and t,2 subscripts:
1) "\widehat{\delta x}_{t,1}=(\hat{x}_t-\hat{x}_{t,1})+K_{t,1}[z_t-\{h(\hat{x}_{t,1},0)+H_{t,1}(\hat{x}_t-\hat{x}_{t,1})\}]"
Color the left delta-hat BLUE and BOTH occurrences of (\hat{x}_t-\hat{x}_{t,1}) ORANGE. Everything else black. Put the whole equation in a very pale blue rounded underline/enclosure like the reference, not a heavy box.
2) "\hat{x}_{t,2}=\hat{x}_{t,1}+\widehat{\delta x}_{t,1}"
Color the result xhat_{t,2} GREEN, current xhat_{t,1} BLUE, and correction BLUE.
Final short caption: "재선형화 → error mean 추정 → nominal 보정 → 반복"

Constraints: all formulas fully visible, minus signs correct, visible hats on every nominal x, no actual unknown true state location claimed. No x_true, no residual shorthand, no S, no zhat, no unexplained F or noise Jacobian, no covariance update inside iteration. Diagram is schematic, do not add numeric truth labels. Keep the original hand-drawn lecture-note aesthetic. No textures, gradients, 3D, clipart, watermark, decorative clutter. Prior-offset arrow RIGHT, correction arrow LEFT; these are intentionally different.

## Final geometry-refinement prompt

Supporting input: a mathematically plotted geometry reference. The final raster edit was generated by the built-in image tool; the reference was not substituted for the deliverable.

Use case: scientific-educational.
Image 1 is the EDIT TARGET. Image 2 is a mathematically exact GEOMETRY GUIDE for its upper plot, NOT a style reference.
Make a precise geometry correction to image 1. Preserve its white background, overall aspect ratio, iPad handwriting, ALL Korean text, both bottom update equations verbatim with orange terms/blue correction/green result, title, arrows, and color palette. Do not restyle it.
ONLY correct the black observation curve and three straight colored tangent segments so they have the same positions and slopes as in image 2. In screen pixel coordinates in the guide the curve is y=335.3992+0.09280348*x-0.0001564493*x^2. Contact positions: green (618,333), blue (1043,262), red (1520,115). Tangent slopes dy/dx: green -0.1005679, blue -0.2335498, red -0.3828024. Scale these consistently into the target's matching plot. Red tangent extends on both sides of red dot, blue on both sides of blue dot, green on both sides of green dot. Each dot must sit EXACTLY on both its curve and its tangent. The black curve must be smooth with NO slope kink at any dot. It is a shallow convex-up curve, not exponential. Let the curve start at guide x=310 to avoid its tiny left turnaround. Do NOT exaggerate the curvature near the red dot. Match the GUIDE rather than keeping the inaccurate slopes from image 1. Preserve text placement with whitespace and prevent label/tangent overlap.
Also complete the red x-axis label to "\hat{x}_{t,0}=\hat{x}_t". This is the same fixed initial prior nominal. All nominal hats must stay visible.
Keep the orange RIGHTWARD arrow exactly from the blue current nominal projection to the red original prior projection, labelled "\hat{x}_t-\hat{x}_{t,1}". Keep the blue LEFTWARD correction arrow from the blue current nominal to the green next nominal; its tip should align exactly with the green dashed projection (not end short). Do not change the mathematical equations or remove any symbols. Do not duplicate axes, labels, or equations. Output one finished wide illustration, same composition as image 1.

